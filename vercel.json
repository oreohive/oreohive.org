{
  "version": 2,
  "builds": [
    {
      "src": "web/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    },
    {
      "src": "api/package.json",
      "use": "@vercel/node"
    },
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/src/routes/$1" },
    { "src": "/(.*)", "dest": "/web/$1" }
  ]
}